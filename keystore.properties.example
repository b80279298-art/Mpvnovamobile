# Copy this file to keystore.properties for local release signing only.
# Do not commit the real keystore.properties or the keystore file itself.
storeFile=keystore/your-release-key.jks
storePassword=change-me
keyAlias=your-key-alias
keyPassword=change-me
